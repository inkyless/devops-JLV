name: Lint Code Base

on: 
  push:
    branches:
    - main
  pull_request:
    branches : 
    - main

jobs:
  build:
    name: Lint Code Base
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      packages: read
      # To report GitHub Actions status checks
      statuses: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Super-linter
        uses: super-linter/super-linter@v7.0.0 # x-release-please-version
        env:
          DEFAULT_BRANCH : main
          VALIDATE_ALL_CODEBASE: true
          VALIDATE_HTML : true
          VALIDATE_JAVASCRIPT_ES	: true
          VALIDATE_PYTHON : true
          # To report GitHub Actions status checks
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    
      - name: Docker Login
        uses: docker/login-action@v3
        with:
              username: ${{ secrets.DOCKERHUB_USERNAME }}
              password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Get Short Git SHA
        id: vars
        run: echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
            
      
      - name: Build and push container image
        uses: docker/build-push-action@v6
        with:
                push: true
                tags: ${{ vars.DOCKERHUB_USERNAME }}/jlv-project:${{ env.sha }}
          


  # deploy:
  #     name: Deploy Docker and Kubernetes
  #     runs-on: ubuntu-latest
                      
  #     permissions:
  #       contents: read
  #       packages: read
  #       # To report GitHub Actions status checks
  #       statuses: write

  #     steps:
  #         - name: Checkout Code
  #           uses: actions/checkout@v3
  #           with:
  #             fetch-depth: 0

  #         - name: Set up Kubectl
  #           uses: azure/k8s-set-context@v1
  #           with:
  #               kubeconfig: ${{ secrets.KUBE_CONFIG_DATA }}

  #         - name: Verify Kubernetes config
  #           run: |
  #             kubectl config view

  #         - name: Verify Cluster Connection
  #           run: |
  #               kubectl cluster-info

  #         - name : Deploy
  #           run : |
  #             kubectl apply -f k8s/deployment.yaml --validate=false
  #             kubectl apply -f k8s/service.yaml --validate=false

  test:
    runs-on: ubuntu-latest
    permissions:
        contents: read
        packages: read
        # To report GitHub Actions status checks
        statuses: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up kind
        uses: helm/kind-action@v1.6.0
        with:
          version: v0.20.0

      - name: Apply Kubernetes Manifests
        run: |
          kubectl apply -f k8s/deployment.yaml
          kubectl apply -f k8s/service.yaml

      - name: Verify Deployment
        run: |
            kubectl get deployments
            kubectl get pods
            kubectl get services

      - name: Wait for Pods to be Ready
        run: |
              kubectl wait --for=condition=ready pod -l app=test-deployment --timeout=480s

      - name: Forward Port for Service
        run: |
              kubectl port-forward service/test-service-cicd 8080:80 &
              curl http://localhost:8080/health

